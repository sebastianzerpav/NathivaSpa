@model CategoryDTO

@{
    ViewData["Title"] = "Categoria";
    
}



<div class="pagetitle">
    <h1>Inicio</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Dashboard">Inicio</a></li>
            <li class="breadcrumb-item active">Categoria</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

<section class="section dashboard">

    <div class="row">

        <h1>@Model.Name</h1>

        <form class="" asp-action="Dashboard" method="GET">
            <input type="hidden" name="Page" value="@Model.PaginatedCategories.CurrentPage" />
            <input type="hidden" name="RecordsPerPage" value="@Model.PaginatedCategories.RecordsPerPage" />

            <div class="input-group mb-3">

                <input type="text" class="form-control border-0 small" placeholder="Buscar.." aria-label="Search" asp-for="@Model.PaginatedCategories.Filter" name="Filter">

                <div class="input-group-append">
                    <button class="btn btn-primary" type="submit">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
            </div>

        </form>
        <div class="row">
            @foreach (var item in Model.PaginatedCategories.List)
            {
                <div class="col-md-5 m-2">
                    <a asp-action="SpaService" asp-route-id="@item.IdSpaService" class="btn btn-outline-success form-control">@item.Name</a>
                </div>
            }
        </div>

        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-center">

                @*Primero*@
                <li class="page-item">
                    <a class="page-link @(Model.PaginatedCategories.CurrentPage == 1 ? "disabled" : "" )"
                       asp-action="Index"
                       asp-route-Page="1"
                       asp-route-Filter="@Model.PaginatedCategories.Filter"
                       asp-route-RecordsPerPage="@Model.PaginatedCategories.RecordsPerPage">
                        &laquo;
                    </a>
                </li>

                @*Anterior*@
                <li class="page-item">
                    <a class="page-link @(!Model.PaginatedCategories.HasPrevious ? "disabled" : "" )"
                       asp-action="Index"
                       asp-route-Filter="@Model.PaginatedCategories.Filter"
                       asp-route-Page="@(Model.PaginatedCategories.CurrentPage - 1)"
                       asp-route-RecordsPerPage="@Model.PaginatedCategories.RecordsPerPage">
                        &lsaquo;
                    </a>
                </li>

                @*Paginas*@
                @foreach (int page in Model.PaginatedCategories.Pages)
                {

                    <li class="page-item">
                        <a class="page-link @(Model.PaginatedCategories.CurrentPage == page ? "active" : "" )"
                           asp-action="Index"
                           asp-route-Filter="@Model.PaginatedCategories.Filter"
                           asp-route-Page="@(page)"
                           asp-route-RecordsPerPage="@Model.PaginatedCategories.RecordsPerPage">
                            @(page)
                        </a>
                    </li>
                }

                @*Siguiente*@
                <li class="page-item">
                    <a class="page-link @(!Model.PaginatedCategories.HasNext ? "disabled" : "" )"
                       asp-action="Index"
                       asp-route-Filter="@Model.PaginatedCategories.Filter"
                       asp-route-Page="@(Model.PaginatedCategories.CurrentPage + 1)"
                       asp-route-RecordsPerPage="@Model.PaginatedCategories.RecordsPerPage">
                        &rsaquo;
                    </a>
                </li>

                @*Último*@
                <li class="page-item">
                    <a class="page-link @(Model.PaginatedCategories.TotalPages == Model.PaginatedCategories.CurrentPage ? "disabled" : "" )"
                       asp-action="Index"
                       asp-route-Filter="@Model.PaginatedCategories.Filter"
                       asp-route-Page="@Model.PaginatedCategories.TotalPages"
                       asp-route-RecordsPerPage="@Model.PaginatedCategories.RecordsPerPage">
                        &raquo;
                    </a>
                </li>
            </ul>
        </nav>

    </div>
</section>